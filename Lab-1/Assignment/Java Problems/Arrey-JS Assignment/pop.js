let arr2 = [1, 2, 3, 4]; // Initial array

// Removing the last element
let removedElement = arr2.pop(); // 4 is removed

console.log(arr2); // Output: [1, 2, 3]
console.log(removedElement); // Output: 4, which was the removed element
