let arr7 = [1, 2, 3, 4, 5]; // Initial array

// Finding the index of element 3
let index = arr7.indexOf(3);

console.log(index); // Output: 2

// If the element is not present, it returns -1
let notFoundIndex = arr7.indexOf(10);
console.log(notFoundIndex); 
